{% block form_rows -%}
    {% set form_rows_count = form | length %}
    
    {% for child in form %}
        {# {%  %} #}
        {# {{ loop.index }} {{ loop.last == true ? 'LAST' : '...' }} #}
        {% set form_rows_last  = loop.last %}
        {# {% set childVars = child.options|merge({ 'loopCnt' : form_rows_count, 'loopLast' : form_rows_last }) %} #}

        {{- form_row(child, { attr : { 'data-last-row' : loop.last } }) -}}
        
    {% endfor %}
{%- endblock form_rows %}