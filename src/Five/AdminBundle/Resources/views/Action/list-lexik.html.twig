{% extends 'FiveAdminBundle::layout.html.twig' %}

{% block content %}

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">{{ options.page.title | default( '' ) }}</h3>
            </div>
            <!-- /.box-header -->    
            <div class="box-body table-responsive">

                {% include 'FiveAdminBundle:Action:alert-dismissable.html.twig' %}

                {{ render( controller('LexikTranslationBundle:Translation:grid') )}}
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    </div>
</div>    
<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true"></div>

{% endblock %}
{# 
{% block js %}
    {{ parent() }}
        <script type="text/javascript">
            $(function() {
                
                $('#example1').dataTable({
                    "bPaginate": true,
                    "bLengthChange": true,
                    "bFilter": true,
                    "bSort": true,
                    "bInfo": true,
                    "bAutoWidth": false
                });
            });
        </script>    
{% endblock %} #}